# Checklist

## 2026-09-04：迁移原站正文并移除图示内容

- [x] 确认 3000 原站与 3010 副本的真实项目路径和数据调用关系。
- [x] 写入并运行预期失败的迁移意图测试（3 项中 2 项按预期失败：旧数据包、固定占位正文）。
- [x] 从原站重新生成最新 `courses.js`。
- [x] 将完整章节文字内容接入 3010 动态渲染。
- [x] 确认正文不渲染教学图、波形图、示意图或图片。
- [x] 运行全部本地测试和静态检查。
- [x] 在 `http://localhost:3010/` 验证科目/章节切换、内容和控制台。
- [x] 记录最终结果、限制和未执行事项。

## 2026-09-04：实践功能区阶段 A（Notebook 实践页）

- [x] 核验主计划、当前数据数量、视图调用链和现有测试基线。
- [x] 建立阶段 A 子计划，确认采用静态步骤页且不扩展顶栏入口。
- [x] 写入并运行预期失败的 Notebook 行为契约测试（3 项中 1 项通过、2 项按预期失败）。
- [x] 实现 Notebook 挂载点、视图切换、步骤勾选和返回正文。
- [x] 运行完整测试、语法检查和静态扫描。
- [x] 在桌面与移动视口完成真实浏览器交互验收。
- [x] 记录阶段结果、限制和下一阶段边界。

## 2026-09-04：实践功能区阶段 B（统一数字/模拟工作台）

- [x] 核验阶段 B 主计划、工作台入口、原组件状态结构和现有测试基线。
- [x] 建立阶段 B 子计划，解决“丢弃草稿/保留草稿”冲突并确定双会话方案。
- [x] 写入并运行预期失败的统一工作台行为契约测试（3/3 按预期失败）。
- [x] 实现 bundle 的双 kind 会话与 `setKind(kind)` 接口。
- [x] 接入 shell 分段切换器和数字/模拟直切逻辑。
- [x] 重新构建 bundle，运行完整测试、语法检查与静态扫描。
- [x] 在桌面与移动视口验证切换、双草稿保留和控制台健康。
- [x] 记录阶段结果、限制和阶段 C 边界。

## 2026-09-04：顶栏工作台入口合并

- [x] 明确“统一成一个”为合并顶栏数字台/模拟台入口，内部切换保持不变。
- [x] 核验当前 HTML、调用方、阶段 B 契约测试与 10/10 基线。
- [x] 写入并运行预期失败的单入口行为测试。
- [x] 合并顶栏入口并更新进入/退出状态同步。
- [x] 运行完整测试与桌面、移动浏览器验收。
- [x] 记录结果和未实施范围。

## 2026-09-04：工作台顶部控制区密度优化

- [x] 核对截图、当前 DOM/CSS、组件调用方与真实宽屏尺寸。
- [x] 建立实施计划，确认只增加 ≥1500px 的 CSS 覆盖。
- [x] 写入并运行预期失败的宽屏布局契约测试。
- [x] 重排运行、实验目标与电路存档三组比例。
- [x] 运行专项、完整测试和静态检查。
- [x] 在超宽桌面、普通桌面与移动端完成真实浏览器验收。
- [x] 记录结果、既有失败与未修改范围。
## 2026-09-05：顶部操作关系整理

- [x] 计划：隐藏能力说明；桌面将仿真和实验同排，存档另起整行，减少三块不等高留白；窄屏沿用换行。
- [x] 核对当前 CSS、入口和基线测试。
- [x] 实施最小 CSS 调整。
- [x] 验证数字/模拟切换、窄屏与完整测试并记录结果（两个既有失败仍保留）。
## 2026-09-05：Git 与工作台风格统一

- [x] 创建 Git 基线及 baseline-3010 标签。
- [x] 计划：沿用现有灰色变量、字体与右对齐布局；圆角面板、胶囊切换、清晰焦点和禁用态。独立保存副本组件以优化按键状态，原站不变。
- [x] 优化按钮与样式，构建工作台。
- [x] 测试和浏览器验收，准备提交优化恢复点。
## 2026-09-05：数字/模拟切换滑块与内容淡入淡出

- [x] 检查 git 分支/工作区/标签，创建修改前恢复标签 `restore-2026-09-05-before-kind-switcher-slider`。
- [x] 编写实施计划（docs/superpowers/plans/2026-09-05-kind-switcher-slider-and-transition.md）。
- [x] 滑块结构：kindSwitcher 合并为共享滑块（thumb + role=tab 等宽两项）。
- [x] 滑块样式：184×44、4px 边距、深灰轨道、深色填充+细描边、180ms cubic-bezier(.22,1,.36,1) 只过渡 transform、文字固定；清理旧互覆盖规则。
- [x] 内容过渡：workbench-entry.tsx 管理 80ms 淡出/120ms 淡入；挂载就绪判断；失败反馈。
- [x] 边界：快速连点最后一次为准；过渡期间内容不可操作；卸载清理；键盘方向键；prefers-reduced-motion 即时。
- [x] 构建 bundle、运行完整测试与语法检查。
- [x] Playwright 桌面/移动验收（双向切换、连点10次、首次/动画中返回/再进入、状态保留、键盘、reduced-motion）。
- [x] 提交逻辑变更并创建优化版恢复标签 `restore-2026-09-05-after-kind-switcher-slider`，汇报结果与限制。
