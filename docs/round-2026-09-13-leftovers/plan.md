# 本轮计划（2026-09-13 遗留问题收尾）

## 范围与边界

- **只改 `personal-workbench-shell-3020`**；不改 3010；不同步、不提交、不推送。
- 不扩写其他章节；不实施演练升级；不顺便重构。
- 保留所有既有改动。归属不明的内容先核实再动。
- 3010 现有唯一未提交改动 `tests/workbench-space.test.mjs`（行尾噪声，非本轮产物）**全程不碰**。

## 现状（本轮开始时的实测）

- 第 1 章：10 小节 / 5 节 / 8 张公式卡 / 10 例题 / 13 检验 / 1 实验。
- 全量测试 170 项通过；构建确定性（bundle 与 3010 逐字节一致）。
- 已知遗留：相关函数时延符号写反、吉布斯"有限项略小于极限"写错、白噪声相关前提缺失、
  公式卡裁切断言只覆盖 390/1440、`narrow-workbench-switcher` 跨断点用例曾在全量运行中偶发失败。

## 本轮要做

1. 相关函数：写清 `R_xy(τ)=0.5cos[2π(τ+0.25)]`、峰值 `τ=−0.25+kT`，并明确"峰值时移与延迟符号相反"。
2. 吉布斯：删掉"有限项略小于 9%"，改为渐近约 **8.95%** + 有限项需具体计算；"收敛的反例"改为"不一致收敛现象"。
3. 白噪声：区分互相关与自相关，写"互相关期望在所有时延处为零 + 有限数据有残差"。
4. 相关函数数值测试（独立积分）：`R_xy(−0.25)≈+0.5`、`R_xy(+0.25)≈−0.5`、峰值按 T=1 s 重复；**符号写错必须让测试失败**。
5. 吉布斯数值测试：N=1/3/5 → 13.66% / 10.02% / 9.42%，较大 N 趋近 8.95%（合理容差）。
6. 公式卡检查覆盖 **390 / 760 / 1440** 三档，断言"无 KaTeX 错误 + 无内容裁切"。
7. 排查 `narrow-workbench-switcher` 跨断点用例偶发失败：重复运行记录次数与现场，区分布局缺陷与时序问题。
8. 针对性测试 + 全量测试 + `npm run build`。
9. 更新核验文档，区分"已验证 / 未验证 / 未复现"。

## 本轮不做

- 不扩写频域 / 复频域 / 相关分析 / MATLAB 各节正文（只在纠错处动最小必要文字）。
- 不改演练页与 `SIGNAL_DEMOS`。
- 不重构课程数据结构。

## 已核实的数值基准（本轮动手前先算过）

| 量 | 数值 | 方法 |
| --- | --- | --- |
| `R_xy(−0.25)` | **+0.5000** | 功率相关 `(1/T)∫_0^T x(t)y(t−τ)dt`，T=1 s，20 万点梯形积分 |
| `R_xy(+0.25)` | **−0.5000** | 同上 |
| `R_xy(τ)` 闭式 | `0.5cos[2π(τ+0.25)]` | 积化和差，数值与闭式逐点一致 |
| 峰值位置 | `τ = −0.25 + kT`（T=1 s） | 数值：τ=−0.25、0.75、1.75 均为 +0.5 |
| 吉布斯 N=1 | **13.662 %** | ±1 方波部分和，跳变量 2，步长 1e−6 |
| 吉布斯 N=3 | **10.021 %** | 同上 |
| 吉布斯 N=5 | **9.418 %** | 同上 |
| 吉布斯渐近 | **8.949 %**（N=501 起稳定） | 同上 |

> 结论：有限项过冲**大于**渐近值 8.95%，原文"有限项略小于"确属错误。
