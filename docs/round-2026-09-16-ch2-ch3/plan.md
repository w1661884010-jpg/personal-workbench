# 本轮计划（2026-09-16 第二章纠错 / 第三章推进）

来源：`docs/superpowers/plans/2026-09-16-ch2-repair-ch3-expansion.md`（外部计划，逐条核验后执行）。

## 范围与边界

- 仅操作 3020；**不改 3010、不同步、不提交、不推送**。
- 3010 现有未提交改动一律不清理、不覆盖、不代为暂存。
- 不重构界面、不新增演练交互、不改数据结构与稳定 ID（除已确认失效的锚点）。
- 不用字数或关键词命中代替数学正确性验证；不把数值支持写成一般性证明。

## 阶段一：第二章纠错（本轮主任务）

10 类修正，逐条先核验、再改，并配独立复算：

| # | 位置 | 待纠正 | 核验结论 |
| --- | --- | --- | --- |
| 1 | `-sampling-time` | `fs≥2fmax` 推出唯一恢复 | **成立**：`sin(2πf_max t)` 在 `fs=2f_max` 下样点全零 |
| 2a | `-sampling-time` | 99/101 Hz "都呈现为 1 Hz" | **成立**：折叠后绝对值同为 1 Hz，零相位正弦样点符号相反 |
| 2b | `check-2` | "只有截止 fs/2 可重建" | **成立**：带宽 B 时 `B<f_c<fs−B` 亦可 |
| 3 | `-sampling-freq` | "时域混叠是截断误差另一种说法" | **成立**：周期求和 ≠ 截断 |
| 4 | `-discrete-time` / `check-9` | 抽取"不可逆"、插值含糊、离散卷积无分段 | **成立**：限带序列可重建；插零≠插值；离散可分段 |
| 5 | `-discrete-desc` / `-dtft` / `-z-relations` | 功率按周期平均、DTFT"⟺ ROC 含单位圆" | **成立**：功率用长时间平均；后者只是充分 |
| 6 | `-dft` / `-fft-apps` / `check-14,18` | 把网格间距当分辨能力 | **成立**：网格 fs/N_fft，分辨能力取决于记录时长与窗 |
| 7 | `-fft-radix2` / 补零例题 | "原有频点全部保留" | **成立**：仅两网格重合处相同（300→1024 只有 4 对） |
| 8 | `-z-relations` | z=e^{sT} 当成连续拉氏换变量、按横带判稳 | **成立**：只联系采样冲激串；z=0 不由有限 s 到达 |
| 9 | 第五节 MATLAB | "无符号 Z 变换"、"j 未定义"、FFT 限基2 | **成立**：Symbolic Math Toolbox 有 ztrans（单边）；i/j 可被覆盖；FFT 不限基2 |
| 10 | 第五节代码 | 补零写成 7 点当 8 点、单边谱无条件翻倍、freqz 未显式给频点 | **成立**：逐条修正 |

## 阶段二：第三章（本轮只做来源核对与大纲）

按计划门禁：**阶段一未验收不进入正文扩写**。本轮若有余量，只做前两项——核实教材来源（扫描件用图像查看）并产出 `docs/signals-ch3-outline.md`。

## 验收口径

- 每条修正都要有"原文 / 问题 / 修正 / 依据"，能独立复算的必须复算。
- 新增 `tests/course-ch2-math.test.mjs`：直接 DFT/IDFT 与直接卷积独立交叉验证，不调用同一实现自证。
- 正文断言与实际 ID 关联；先用失效表述确认测试会失败，再改内容。
- 定向测试 → `npm test` → `npm run build` → 390/760/1440 三档目视。
