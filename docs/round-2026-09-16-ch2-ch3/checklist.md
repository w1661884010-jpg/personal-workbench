# Checklist（2026-09-16 阶段一：第二章纠错）— 验收

## Global Constraints
- [x] 仅操作 3020；未改 3010、未同步、未提交、未推送
- [x] 3010 既有未提交改动未被清理、覆盖或代为暂存（本轮未触碰 3010）
- [x] 开始前更新 plan / checklist / context-notes，存档既有状态与本轮文件清单
- [x] 不用字数或关键词命中代替数学正确性验证（新增独立数值复算）

## 阶段一：第二章纠错（10 类）
- [x] 1 采样临界条件：改为 `f_s>2f_max`，等号用 `sin(2πf_max t)`（样点 `sin(πn)≡0`）反例说明；同步公式 / 要点 / 易错 / 检验解析
- [x] 2a 采样与重建措辞：说明折叠保留频率**绝对值**，99 Hz 与 101 Hz 的余弦样点相同、正弦样点**符号相反**
- [x] 2b 重建滤波器：check-2 改为区间说法 `B<f_c<fs−B`，`fs/2` 降为干扰项，解析点明"不是唯一"
- [x] 3 频域采样：删除"截断误差"说法，补 `x_N[n]=Σ_r x[n+rN]`，与 `fft(x,N)` 的截断区分；补"必须已知长度与支撑位置"
- [x] 4 抽取/插值：反折≠抽取；抽取"对任意序列一般不可逆"+限带例外；插零与插值滤波分开；删除"没有分段函数"
- [x] 5 功率与 DTFT：功率改为长时间平均定义（周期平均只是周期序列的简化）；DTFT 绝对可和写成充分条件；ROC 含单位圆写成充分条件并注明周期序列用广义谱
- [x] 6 记录长度 L 与 FFT 长度：网格间距 `fs/N_fft` 与分辨能力（记录时长 `T_rec=L/fs` + 窗主瓣）分开；修 dft/fft-apps 公式变量、例题、check-14/18
- [x] 7 补零频点：删除"原频点全部保留"，给出 300→1024 的 4 对重合点与 256→1024 的整数倍条件
- [x] 8 s 与 z：改为"采样冲激串的拉普拉斯 ↔ 序列 Z 变换"，给 `e^{−t}u(t)` 反例；横带映到**非零** z 平面、z=0 不由有限 s 到达；稳定性不需选横带；区分 ω 与 Ω；判稳限定因果有理 LTI，零极点图不替代 ROC
- [x] 9 MATLAB 知识：`ztrans/iztrans` 存在且为单边；`i/j` 是可被覆盖而非未定义，建议 `1j`；`fft` 不要求 N 为 2 的幂，耗时比 ≠ 运算量比
- [x] 10 MATLAB 代码：卷积示例统一 `fft(x,8)`/`fft(h,8)` 并比对前 5 点与尾部；单边谱的实序列条件与直流/Nyquist 不翻倍；负频按映射轴说明；`freqz(1,[1 -0.5],[0 pi])` 显式取端点；系数按 z 的非正幂递增排列

## 验证
- [x] 新增 `tests/course-ch2-math.test.mjs`：6 项独立复算（含直接 DFT/IDFT 与直接卷积三方交叉）+ 6 项正文一致性，全部绑定实际 ID
- [x] "先失败后修正"：词元级变异注入 10 类旧表述 → **5/7 条文本一致性测试失败**（另 2 条未涉及被变异词元），证明断言非空转；文件按 SHA256 原样还原
- [x] 定向测试 → 全量测试 **184 passed / 0 failed**
- [x] `npm run build` 通过
- [x] 390 / 760 / 1440 三档：5 节、18 小节、13 公式卡、0 KaTeX 错误、0 裁切、18 例题、21 检验、0 横向溢出、答案可展开、选项可作答、0 pageerror

## 阶段二：第三章按来源推进（本轮完成）
- [x] 来源核对：用**页面图像**逐页核对教材实物（PDF 第 9/10/11 页 = 书内 Ⅶ/Ⅸ/X），**不把"无文本层"当借口**
- [x] 反查第二章：教材 18 个子目与站点 18 个小节**逐一对应**（5+3+3+2+5），页码 109–188
- [x] 产出 `docs/signals-ch3-outline.md`：五节 15 子目 + 页码 + 现有 id 处置表 + 硬性内容要求
- [x] 第一节：系统的描述 / 系统的性质（含 y=n·x 与 y=x² 两个反例逐项判断）
- [x] 第二节：时域法 / 频域法 / 复频域分析（零输入·零状态·全响应分离；Y=HX 前提）
- [x] 第三节：系统辨识 / 逆滤波 / 同态解卷积（H=1−2z^{−1} 反例：原系统稳定、因果逆不稳定）
- [x] 第四节：数字处理特点 / 实现 / 有限字长（|Q−x|≤Δ/2 确定性界；Δ²/12 的假设）
- [x] 第五节：MATLAB 时域 / 频域 / 复频域 / 系统辨识（filter 的 zi 需 filtic 换算）
- [x] 演练文案歧义修正：「观察单位阶跃输入的前 20 点」+ 20 点矩形停输入后衰减的说明
- [x] 退役 3 个旧 id，改指 4 处引用（ch1×2、ch2×2），无悬空锚点
- [x] 新增 `tests/course-ch3-math.test.mjs`（11 项：5 独立复算 + 6 正文一致性）
- [x] 全部小节完成后才把 `signals-ch3` 加入棘轮名单（未提前降门槛）
- [x] 全量测试 **195 passed / 0 failed**；构建通过；390/760/1440 三档公式卡无裁切、无溢出

## 阶段三：第四章「滤波器」（本轮完成）
- [x] 来源核对：教材实物目录（PDF 第 11 页 = 书内页 X）→ 四节 13 个子目，页码 261–320
- [x] 产出 `docs/signals-ch4-outline.md`：四节 13 子目 + 页码 + 现有 id 处置表 + 核验过的数值基准
- [x] 第一节：滤波及滤波器的基本原理 / 滤波器的分类 / 滤波器的技术要求
- [x] 第二节：模拟滤波器概述 / 巴特沃思 / 切比雪夫 / 频率变换 / RC 有源滤波器
- [x] 第三节：数字滤波器概述 / IIR / FIR
- [x] 第四节：模拟滤波器设计 / 数字滤波器设计（MATLAB）
- [x] 退役 2 个旧 id（`-analog`、`-advanced-design`），无悬空锚点
- [x] 新增 `tests/course-ch4-math.test.mjs`（11 项：5 独立复算 + 6 正文一致性）
- [x] 全部小节完成后才把 `signals-ch4` 加入棘轮名单
- [x] 全量测试 **206 passed / 0 failed**；构建通过；390/760/1440 三档 4 节 13 小节 8 卡，0 裁切、0 溢出

## 阶段二由测试发现并修正的自身错误
- [x] 「零输入 + 零状态」例题与正文的**全响应解析式写错**：原写 `2−1.5×0.5^n`，正确为 `2−0.5^(n+1)`
      （`2(1−0.5^(n+1)) + 0.5^(n+1) = 2 − 0.5^(n+1)`；n=0 处应为 1.5，旧式给 0.5）
      由新增测试的独立复算捕获
## 本轮由测试发现并修正的自身错误
- [x] **巴特沃思 Ωc 与 Ωp 的关系写错**：原文写"Ωc 取 Ωp（此时通带边界恰好满足 Ap）"。
      正确关系是 Ωc 由"使 Ωp 处衰减恰为 Ap"确定，Ap=1 dB（<3.0103 dB）时 **Ωc 略大于 Ωp**；
      只有 Ap 恰为 3.0103 dB 时二者才重合（Ωc=1.0881 rad/s，实测 Ωs 处 42.297 dB）。
      已修正正文 detail、例题步骤说明与答案三处。

## 未完成 / 未核实
- [ ] MATLAB 命令未实际执行（本机无 MATLAB），只做 Node 等价数值验证
- [ ] 教材各节的**正文内容**未逐页阅读，仅核对了目录页与页码


## 阶段四：第五章（本轮完成）
- [x] 来源核对：PDF 第 12 页（书内页 XI）补全目录 → 五节 19 个子目，页码 321–399
- [x] 五个节 19 个小节全部扩写（含最优线性滤波三小节与非平稳三小节）
- [x] 退役 3 个旧 id（-lti-random、-optimal、-nonstationary），无悬空锚点
- [x] 新增 	ests/course-ch5-math.test.mjs（12 项）
- [x] 章节联系图补成对：ch1/ch3 增加 
ext → signals-ch5
- [x] 全量 **218 passed / 0 failed**；构建通过；三档 5 节 19 小节 10 卡，0 裁切、0 溢出
- [x] 由测试发现并修正：正文引用了简单 LCG 的实测数字（0.0274/9.1546），已改为理论波动量级

## 五章总览（本轮结束时的状态）
- 第1–5章：**75 个小节 / 75 例题 / 90 检验 / 47 公式卡**，全部进入棘轮名单
- 下一步：**演练部分优化**（未开始）