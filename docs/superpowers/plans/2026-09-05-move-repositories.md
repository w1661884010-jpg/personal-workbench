# Move two workbench repositories
Goal: move only personal-workbench-sites and personal-workbench-shell into Desktop/learning/repositories with -3000 and -3010 folder suffixes. Keep complete Git/config/dependencies and website behavior.
Steps: verify clean Git and absent targets; stop only confirmed 3010 preview process; update sibling build/import paths; Move-Item using validated literal absolute paths; restart servers hidden from new roots; run both test suites and shell builds; verify refs and absence of old folders.
No deletion, no junctions, no unrelated repository moves, no automatic remote deployment.
