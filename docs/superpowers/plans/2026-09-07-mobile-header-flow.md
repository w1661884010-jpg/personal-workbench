# 移动端顶栏与正文错位修复

- 范围：仅 3010，提交本地、不推送。基线 fefe0a6，工作区干净。
- 证据：固定顶栏、独立固定分隔线、208px 正文占位；滚动收起同时改变正文 padding。手机截图存在重叠及绘制残留，本机尚未复现同款 WebView 残影。
- 方案：≤760px 改用正常流内 sticky 顶栏，取消 transform 收起及正文 padding 动画；顶栏自身边框替代独立固定分隔层。桌面保持原行为。不禁止用户字体缩放。
- [x] 先增加真实浏览器测试：动态行高后正文不被顶栏遮住；滚动/视图切换稳定；桌面仍保留收起行为。修复前 5/5 失败。
- [x] 实施 CSS 局部覆盖及 JS 移动端滚动守卫。
- [x] 新增 5/5、完整 90/90 通过；构建、截图核验、diff-check 通过。
- [x] 单独 fix 本地提交，不执行 push。手机微信真机绘制仍需用户复核。
