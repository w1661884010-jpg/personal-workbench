# 第二章纠错与第三章推进 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> 若执行环境没有上述技能，不声称调用成功；按本文检查点顺序执行。此任务交给 DeepSeek，不要求创建子代理。

**Goal:** 修复第二章可确认的知识错误，通过独立复算后，按教材依据逐节充实第三章。

**Architecture:** 保留 courses.js 现有 sections/examples/check/experiments 数据结构及稳定 ID。内容纠错与第三章扩充独立验收；不重构界面、不新增演练交互。

**Tech Stack:** 静态 JavaScript、KaTeX、Node test、现有浏览器测试；MATLAB 示例单独核验工具箱要求。

## Global Constraints

- 仅操作 C:/Users/Lenovo/Desktop/learning/repositories/personal-workbench-shell-3020。
- 不修改 3010，不同步、不提交、不推送；用户另行确认后才能发布。
- 3010 当前有多项既有未提交改动，不得清理、覆盖或代为暂存。
- 开始实施前更新 plan、checklist、context-notes，保存既有状态与本轮文件清单。
- 不用字数或关键词命中代替数学正确性验证；不把“数值支持”写成一般性数学证明。
- 本计划审阅了第二章18小节、18例题、21检验题和第三章现状；未逐页核对教材原件，目录和资料页码须另行查证。

## 阶段一：第二章纠错

文件：修改 courses.js 的 signals-ch2；新增 tests/course-ch2-math.test.mjs；扩展 tests/course-content-render.test.mjs。记录放 docs/round-2026-09-16-ch2-ch3/。
输入：当前第二章数据。输出：正文、公式卡、例题、检验、总结一致的修正版；保持现有 ID。

- [ ] 采样临界条件：signals-ch2-sampling-time 的 fs≥2fmax 不得无条件推出唯一恢复。主线采用 fs>2fmax，等号单独说明端点频谱/信号类别条件；用 sin(2πfmax t) 在 fs=2fmax 时样点全零作反例。同步 check-3、总结与公式。
- [ ] 采样与重建措辞：99Hz、101Hz 在100Hz采样下仅折叠频率的绝对值同为1Hz，零相位正弦样点符号相反。check-2 删除“只有截止fs/2才可重建”：带宽B<fs/2时，适当截止 B<fc<fs−B、通带增益T也可恢复。
- [ ] 频域采样：signals-ch2-sampling-freq 删除“时域混叠是截断误差另一种说法”。补周期求和 x_N[n]=Σ_r x[n+rN]；与 fft(x,N) 在N小于数据长度时直接截断区分。唯一恢复需已知长度及支撑位置。
- [ ] 抽取/插值：signals-ch2-discrete-time 与 check-9 的“不可逆”改为“对任意序列一般不可逆”；限带序列无混叠抽取后可以插值重建。反折a=−1不是丢点抽取；整数抽取M>1与插零L>1分开写。插零定义为整除处x[n/L]、其他位置0，再说明插值滤波。删除“离散卷积没有分段函数”，离散序列也可分段表达。
- [ ] 功率与DTFT：功率使用长时间平均定义，周期内平均只是周期序列的简化；注明非零有限功率条件。signals-ch2-z 将“DTFT要求绝对可和”改为充分条件；ROC含单位圆可保证绝对收敛的DTFT，不写成所有意义下DTFT存在的充要条件。说明周期序列可使用广义谱。
- [ ] 区分记录长度L与FFT长度Nfft：网格间距fs/Nfft，记录时长L/fs；分辨能力与记录长度及窗主瓣等有关，不统一写成补零后的fs/Nfft。修正 dft/fft-apps 公式变量、相关例题与 check-14/18。零填充改善频率取样密度而非两近频分辨能力；更换窗也影响分辨能力。不要说FFT应用“只有两类”。
- [ ] 补零频点：fft-radix2及300→1024例题中，删除“所有原频点均保留”。只有两网格重合处数值相同；Nfft为L整倍数时才全部保留。300→1024仅四对频点重合：(0,0)、(75,256)、(150,512)、(225,768)。256→1024例子可保留，使用容差而非浮点逐字节相等。
- [ ] s与z关系：signals-ch2-z-relations 明确 z=e^(sT) 将采样冲激串的拉普拉斯与序列Z变换联系起来，不是原连续信号的拉普拉斯表达式直接换变量。横带映至非零z平面，z=0不由有限s到达；稳定性不需任选某条横带。数字频率ω与连续角频率Ω分开。极点判稳限定因果、有理LTI输入输出系统；零极点图本身不能替代ROC。
- [ ] MATLAB知识纠错：删除“MATLAB没有符号Z变换”，注明Symbolic Math Toolbox提供ztrans/iztrans，且ztrans是单边定义；不要据此验证任意双边左序列ROC。删除“j未定义”，改为i/j可能被变量覆盖，优先1j。fft不限基2，实际耗时比不保证等于运算量比。
- [ ] MATLAB代码修正：卷积示例当前长度为4+3=7、2+5=7，并非8；7也足够计算线性卷积，但应统一写成fft(x,8)、fft(h,8)，比较前5点并验证尾部近零。不同长度数组不能直接fft相乘。单边谱对实序列才可如此构造，偶数N时直流和Nyquist均不翻倍，奇数N无Nyquist端点。负频率区按映射轴说明，不是“无效频率”。freqz显式传[0 pi]读取端点，系数按[b0,b1,...]对应z的非正幂递增排列。

### 最小复算与先失败后修正

- [ ] 新增数值用例并绑定实际章节断言，先确认现有错误表述使对应测试失败；随后修改内容再运行。独立数学代码正确不能单独证明正文正确。

```js
// 放入Node测试；其他用例沿用项目assert与vm读取courses.js方式。
assert.ok(Math.max(...Array.from({length:32},(_,n)=>Math.abs(Math.sin(Math.PI*n)))) < 1e-12);
assert.deepEqual([1+3,2+4],[4,6]); // DTFT取2点后逆变换的周期求和
assert.notDeepEqual([4,6],[1,2]); // 不等于截断
const common=[];
for(let k=0;k<300;k++) if(k*1024%300===0) common.push([k,k*1024/300]);
assert.deepEqual(common,[[0,0],[75,256],[150,512],[225,768]]);
```

- [ ] 用直接DFT/IDFT与直接卷积独立交叉验证，不调用同一个实现自证：x=[1,1,1,1]、h=[1,1]，线性结果[1,2,2,2,1]；4点循环结果[2,2,2,2]；8点补零结果前5点同线性、后3点近零，容差1e-10。
- [ ] 单边谱测试偶数N的全1、(-1)^n及整频余弦，幅度均为1；另加奇数N案例，捕获误翻倍或漏翻倍。
- [ ] 实际执行以下MATLAB代码；没有MATLAB时仅报告Node等价数值验证，不宣称MATLAB运行通过。

```matlab
x=[1 1 1 1]; h=[1 1];
y=conv(x,h); yf=ifft(fft(x,8).*fft(h,8));
assert(max(abs(yf(1:numel(y))-y))<1e-10);
assert(max(abs(yf(numel(y)+1:end)))<1e-10);
H=freqz(1,[1 -0.5],[0 pi]);
assert(max(abs(abs(H)-[2 2/3]))<1e-10);
```

- [ ] 验证复频域转换反例：xc(t)=exp(-t)u(t)，连续变换1/(s+1)，采样序列Z变换1/(1-exp(-T)z^-1)；不得用s=log(z)/T直接替换连续表达式声称两者相等。
- [ ] 定向测试通过后执行 npm test 与 npm run build；390/760/1440打开第二章全部公式、例题、检验区，检查渲染、裁切、横向溢出、答案反馈。记录初次失败及最终结果，不以单项复跑掩盖全量失败。
- [ ] 第一阶段提交验收报告；未完成或有实质失败，不进入第三章正文扩写。

## 阶段二：第三章按来源推进

文件：新增 docs/signals-ch3-outline.md；修改 courses.js 的 signals-ch3；新增 tests/course-ch3-math.test.mjs；更新内容与渲染测试。仅必要时调整失效锚点映射，先查调用方。
输入：第二章已验收内容、实际教材目录与配套资料。输出：可自学、可复算的第三章，不更改演练架构。

- [ ] 先核对教材版本、目录原页及课件范围，列“教材子目/现有ID/保留或拆分/资料页码/验证算例”。扫描件用图像查看，不能把无文本层当成无法核对；未找到的出处标待核实，不编页码或套verified_local。
- [ ] 保留五节框架作为待来源核实的起点：系统性质、线性系统处理、解卷积、数字实现、MATLAB。先写大纲，不把当前5个短段落认作完整覆盖。
- [ ] 第一节：系统描述、线性/时不变/因果/无记忆/BIBO稳定/可逆逐项判断。至少覆盖y[n]=n*x[n]（线性、时变、无记忆、因果但非BIBO稳定，n遍历无界整数）与y[n]=x[n]^2（非线性、时不变、因果、无记忆且BIBO稳定）的反例。
- [ ] 第二节：区分零输入/零状态/全响应；只有零状态关系才能无条件写Y=HX。连续卷积、离散卷积、差分/微分方程、频响、ROC、因果稳定判据互相对照。固定样例y[n]=x[n]+0.5y[n−1]：初始静止阶跃响应为2(1−0.5^(n+1))，h[n]=0.5^n u[n]；另设y[-1]=1验证零输入项0.5^(n+1)。
- [ ] 修正现有演练文案歧义：“长度20的单位阶跃”改为“观察单位阶跃输入的前20点”；若输入确为20点矩形，必须说明停输入后输出衰减，不能声称永久趋于2。本阶段只修文案及匹配验证，不重做交互。
- [ ] 第三节：区分系统辨识H=Y/X与逆滤波X=Y/H；说明激励不足、频谱零点、噪声放大和因果稳定逆的条件。使用H(z)=1−2z^-1说明原系统FIR稳定，但因果逆1/(1−2z^-1)不稳定；不能仅以“非零即可除”判断可实现性。正则化与同态方法按教材范围决定深度，不擅自铺开算法。
- [ ] 第四节：采样/量化/运算/输出链路，区分系数量化、舍入、饱和与回绕。采用舍入量化Q(x)=Δ*round(x/Δ)，无溢出条件下验证|Q(x)−x|≤Δ/2；Δ²/12仅在均匀误差等假设下给出，不能写成无条件噪声定律。
- [ ] 第五节：给独立可执行MATLAB脚本与工具箱清单；filter递推、conv核对零状态响应、freqz显式频点验证、量化误差验证。非零初值下filter的zi是内部状态，不直接等同于任意方程初值。同态解卷积不得替代整节MATLAB工作流。
- [ ] 每个实质小节包含“问题引入→定义与条件→推导或算例→要点→易错点→关联”，按需要配置公式卡。每个小节至少一道对应分步例题与理解检验；不为凑字数重复结论。
- [ ] 第三章数值测试覆盖递推/卷积/解析式三方一致、非零初值分解、逆系统不稳定反例、量化误差界；正文断言与实际ID关联，避免仅测试独立硬编码公式。
- [ ] 每完成一节做内容、链接和公式验证。全部完成后再把signals-ch3加入内容充实指标名单，不提前降低门槛或把未写节标为完成。
- [ ] 最终运行完整测试、构建与三档视觉检查；保持原演练入口、目录锚点、章节联系和检验进度兼容。发生既有偶发失败须单独披露并定位，不无关重构。

## 交付格式

- 两阶段分别列实际改动文件、错误原文与修正、来源、独立复算结果、测试/构建/视觉证据。
- 标明未运行的MATLAB命令、未核实教材页码、失败与剩余疑点。
- 最终确认仅停留3020，未同步、未提交、未推送。

## 官方参考

- https://www.mathworks.com/help/matlab/ref/fft.html
- https://www.mathworks.com/help/signal/ug/amplitude-estimation-and-zero-padding.html
- https://www.mathworks.com/help/symbolic/sym.ztrans.html
- https://www.mathworks.com/help/signal/ref/freqz.html
