# README 使用截图

在独立浏览器会话中打开本地 3010，实拍课程、演练、工作台三种入口。截图随 README 纳入 docs/images/，不包含私人存档，不改变页面 UI。检查图片可读、相对链接存在、文案与实际控件一致；运行现有测试及发布构建。不提交、不推送。

Browser plugin not available；采用现有 Playwright/Chrome 环境。流程：课程首页 → 演练参数与波形 → 工作台元件与画布。
