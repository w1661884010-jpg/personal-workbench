# Checklist（2026-09-16 演练模块改进）

来源：桌面 `信号与系统前两章演练模块工程规划与验收规范.md`（176 行执行版）。
**本清单与 `plan.md`、`context-notes.md` 为同轮独立段，不改动此前各轮记录。**

## 任务0：基线与记录（完成前不编码）—— ✅ 已完成
- [x] 读取 3020 目录结构与关键调用方（**AGENTS.md 实际不存在**，见 context-notes D3）
- [x] 记录基线：3010 = `4973369`、3020 无独立 Git、两侧运行文件哈希一致
- [x] 历史 229/229 **不作为本次已通过证据**——已按 `npm test` 语义重新运行
- [x] 先写本批（A）的数值反例与行为用例，**已确认因缺少 `window.__practiceCalc` 而失败**（正确原因）

### 基线复跑（本轮重新执行）
- [x] `npm test`（`node --test --test-concurrency=1 tests/*.test.mjs`）→ **229/229 通过，338 s**
- [x] `npm run build` → 通过（0.7 s）
- [x] `node --check app.js` → 通过
- [x] 无失败、无跳过、无取消（不称"全绿"以外的说法）

### 批次 A 的测试（先失败后实现）
- [x] `tests/practice-convolution.test.mjs` 已写（6 个用例）
- [x] `tests/practice-sampling.test.mjs` 已写（6 个用例）
- [x] 契约：`window.__practiceCalc`（纯计算）+ `window.__practiceState(id)`（状态），见 context-notes D2
- [x] 已确认当前失败原因为"缺实现"而非"选择器错"（context-notes D6）

## 批次 A：模块 2（动态卷积）+ 模块 4（采样重建）

### 模块 2 `signals-ch1-convolution`（原 ID 升级）—— ✅ 已完成
- [x] 数据条目：title 改「连续卷积与图解五步动态演示」，更新 goal/steps/expected（不动 limitation 作为 UI 依据）
- [x] 首版仅单位高度矩形，支撑 [0,w₁]、[0,w₂]，端点半高
- [x] 预设 (1,1)/(1,2)；w∈[0.5,3] 步长 0.1；t∈[-1,w₁+w₂+1] 步长 0.02 默认 -1
- [x] 计算：`a=max(0,t-w₂)`、`b=min(w₁,t)`、`y=max(0,b-a)`；支撑 [0,w₁+w₂]；峰值 min(w₁,w₂)；总面积 w₁w₂
- [x] 播放/暂停/复位（复位回 t=-1 并暂停）；改宽度后夹紧 t
- [x] 上图画 x(τ)、h(t-τ)、乘积阴影；下图 y(t)+游标；点击历史位置显示 τ 与 t-τ
- [x] 数值对照：[-1,w₁+w₂+1] 上 dτ=0.005 复合梯形，绝对误差 < 0.01；测非网格对齐 t 与跳变前后
- [x] 验收：t=-1 为 0；(1,1) t=1 峰值 1；(1,2) [1,2] 平台 1；总面积 1 与 2；退出后无旧回调
- [x] 解释题与预测问题、≥2 组反例对照
- [x] `tests/practice-convolution.test.mjs`：调用实际实现，与解析式/手算数组对照

### 模块 4 `signals-ch2-aliasing`（原 ID 升级）—— ✅ 已完成
- [x] 数据条目：title 改「时频双域采样、混叠与模拟重建」，更新 goal/steps/expected
- [x] f_in∈[1,15] 整数默认 7；fs∈[2,30] 整数默认 20
- [x] 重建：sinc `Σ x[n]sinc(fs·t-n)`、ZOH 用 `floor(fs·t)`、线性连相邻样点；n=-32…32
- [x] RMSE 在 t_i=-0.5+i/1000（i=0…1000）等权计算；注明有限截断误差
- [x] 观测频率 `min(r, fs-r)`，`r=f_in mod fs`；频域箭头 ±f_in+k·fs，合并重合位置并标低通通带
- [x] fs>2f_in 显示满足严格条件；等号显示「临界，依赖相位，不保证一般恢复」；小于显示混叠
- [x] 术语：2f_in 叫「所需采样率临界值」，fs/2 叫「奈奎斯特频率」
- [x] 验收：7/20 sinc RMSE<0.01、ZOH>0.5；7/10 观测 3Hz 且 7Hz/3Hz 样点差≤1e-10；7/14 显示临界
- [x] 解释题与预测问题、≥2 组反例对照
- [x] `tests/practice-sampling.test.mjs`：调用实际实现复算

### 批次 A 的守卫与同步
- [x] `tests/notebook-practice-page.test.mjs`：新增 id/title/limitation 非空断言与实验 ID 查重（**该文件没有 index()，另建 Set**）
- [x] `experiments.length` 由 6 同步为 **6**（模块 2/4 为原地升级，数量不变）——已核对
- [x] `tests/practice.behaviour.test.mjs` 的写死预期按 ID 同步
- [x] 保留 `tests/course-content.test.mjs` 的占位符与内容守卫

## 每批验收（文档第五节）
- [x] 数值覆盖默认、两端、临界值与非默认组合
- [x] 390/760/1440px × 浅/深主题逐模块打开：无横向溢出、图轴/图例/数值卡/焦点正常，移动端纵排（12 张截图见 `screenshots/`）
- [x] Playwright 操作真实控件验证数据更新；快速切换、播放离开、重进、resize、主题变更、状态保留
- [x] reduced-motion 不自动播放且单步可用；无 console 错误、旧帧覆盖、重复计时器
- [x] 报告：文件清单、复算参数、测试数量、截图、性能测量环境、未验证项（`report-batchA.md`）
- [x] **关键数学失败 → 停止下一批**，不放宽容差掩盖（无关键数学失败；变异测试缺口已补齐，见下）

### 批次 A 收尾补充记录
- 变异测试 4 项：① 峰值 min→max **被抓住**；② 反折公式改错 **被抓住**；③ sinc 重建退化为最近样点 **被抓住**；④ 删除端点半高 **首轮未被抓住** —— 文档容差 0.01 覆盖了它（对齐情形丢半高只差 0.0025）。
  已补断言：端点必须取 0.5，且对齐情形数值积分必须等于 `解析值 − dτ/2`（=0.9975，丢半高则为 1.0）。复验变异 ④ → **被抓住（fail=1）**，`app.js` 还原后 SHA256 与变异前一致。
- 覆盖缺口补齐：规划文档 §三.2「点击历史位置显示 τ 与 t−τ」原先无任何测试。已补用例，断言读数自洽（τ 落在输入支撑内、已历经时间 = t−τ、h(t−τ) 与计算契约独立取值一致）。
  该读数仅由点击处理函数写入（app.js:2758/2760），删除处理函数即失败。

## 批次 B：模块 5（频谱分析）+ 模块 6（循环卷积）—— ✅ 已完成

### 前置：规格值独立复算（复算通过后才写测试）
- [x] L=16 局部峰 1 个 @ 11.08 Hz；L=64 两个 @ 9.89 / 12.14 Hz（与文档 §五 一致）
- [x] 矩形窗第一旁瓣 −13.25 dB、Hann −31.47 dB；Hann 主瓣 2.032 Hz > 矩形 1.000 Hz
- [x] (4,4,N4) 线性 [1,2,3,4,3,2,1]、循环 [4,4,4,4]、E=6；N7 一致；N10 尾部 3 零
- [x] (8,8,N2) [32,32]、折回 13、受影响桶 2

### 模块 5 `signals-ch2-spectral-leakage`（第二章新增）
- [x] 数据条目：title「观察时间、窗与补零分别改变了什么」，含预测题、2 组反例、解释题、expected、limitation
- [x] f_s 固定 64 Hz；L∈[16,128] 步长 8 默认 32；M∈[0,224] 步长 16 默认 0；N_fft=L+M
- [x] 场景：10 Hz、10.5 Hz 单音与 10+12 Hz 等幅零相位双音；窗：矩形与对称 Hann `w[n]=.5−.5cos(2πn/(L−1))`
- [x] 归一化 `|Σw[n]x[n]e^{−j2πfn/fs}|/Σw[n]`，不作单边翻倍（单位幅度单音谱峰 = 0.5）
- [x] 指标：L/f_s、f_s/L、补零后网格 f_s/N_fft；谱峰位置；窗主瓣边界/第一旁瓣
- [x] 下图单独画窗自身归一化频谱：首次局部极小=主瓣边界，其后首个局部极大=第一旁瓣
- [x] 验收：补零不改同频率谱值、DFT 点与直接求和 ≤1e-9、L16 一个峰、L64 两峰在指定区间、旁瓣区间与主瓣宽度
- [x] `tests/practice-leakage.test.mjs`（6 用例）

### 模块 6 `signals-ch2-circular-convolution`（第二章新增）
- [x] 数据条目：title「循环卷积：尾部如何绕回头部」，含预测题、2 组反例、解释题、expected、limitation
- [x] N₁/N₂∈[2,8] 默认 4；N∈[2,16] 默认 4；观察 m∈[0,N−1] 默认 0 且带**单步按钮**（到边界停住不回绕）；改 N 后夹紧 m
- [x] 先算线性 y、L=N₁+N₂−1，再按 n mod N 累加；DFT 核对**先按模 N 折叠**两个输入
- [x] 左图线性结果与折回箭头；右图 N 刻度显示 x_N[k]·h_N[(m−k) mod N] 与贡献和（≥620px 并排，窄屏纵排）
- [x] 指标区分折回样本 max(0,L−N) 与受影响桶 min(N,max(0,L−N))；E 按「首 N 点误差」定义，超出线性长度取 0；浮点归零阈值 1e-9
- [x] 验收：文档三组手算结果逐项一致；DFT 折叠路线与直接路线一致、截断路线不一致
- [x] `tests/practice-circular.test.mjs`（6 用例）

### 批次 B 的守卫与同步
- [x] `tests/notebook-practice-page.test.mjs`：`experiments.length` 6 → **8**
- [x] `tests/practice.behaviour.test.mjs`：短/全标题与 ID 清单改为 8 项；两处下标循环改按 ID 路由
- [x] `tests/narrow-practice-layout.test.mjs`：新增 `openTabById`，卷积用例不再依赖下标
- [x] 保留 `tests/course-content.test.mjs` 的占位符与内容守卫

## 每批验收（文档第五节）
- [x] 数值覆盖默认、两端、临界值与非默认组合
- [x] 390/760/1440px × 浅/深主题逐模块打开：无横向溢出、图轴/图例/数值卡/焦点正常，移动端纵排（本批 12 张截图，累计 24 张）
- [x] Playwright 操作真实控件验证数据更新；快速切换、播放离开、重进、resize、主题变更、状态保留
- [x] reduced-motion 不自动播放且单步可用；无 console 错误、旧帧覆盖、重复计时器（批次 A 已验；本批两模块无播放循环）
- [x] 报告：文件清单、复算参数、测试数量、截图、性能测量环境、未验证项（`report-batchB.md`）
- [x] **关键数学失败 → 停止下一批**，不放宽容差掩盖（无关键数学失败；4 项变异全部被抓住，见下）

### 批次 B 收尾补充记录
- 变异测试 4 项**全部被抓住**：M5a 补零改变 DTFT（乘 L/N_fft）、M5b 归一化分母改用 L、M6a 循环折叠改截断、M6b 受影响桶漏掉 min(N,…)。`app.js` 还原后 SHA256 一致。
- 性能：模块 5 上限（L=128、M=224）同步重绘 14.9 → **8.1 ms**（新增 `spectrumBasis` 缓存，数值不变）；模块 6 上限 0.6 ms。
  切到最大参数时有 73/217 ms 长任务，页面内同步处理仅 ~9 ms，**其余未归因完成**，已在报告第七节列为未验证项。
- 测试自身缺陷修正 4 处（下标路由 2 处、指标期望写错 1 处、字段查找串过宽 1 处），均非产品缺陷。
- 图例类名历史冲突：既有 6 处「标签自带符号 + CSS ::before」重复渲染，新模块只使用有 CSS 支撑的类；
  既有重复不在本批范围，已在报告第四节披露。

## 批次 C：模块 1（波形变换）+ 模块 3（傅里叶表示）—— ✅ 已完成

### 前置：规格值独立复算（复算通过后才写测试）
- [x] a=2,b=−4 支撑 [2,3.5]；a=−1,b=0 支撑 [−3,0]；特征点代回误差 = 0
- [x] T=2 / T=8 谱点数 17 / 65；谱点落在包络上偏差 = 0；T=4,k=2 → c₂=1/(2π) 精确
- [x] T=4,N=101 → x_N(0)=1.003225（≤0.02）；T=2,N=101 过冲 8.951%（∈[8.8,9.2]）
- [x] 渐近线 1.08949：N=11/51/101 峰值 1.090651/1.089551/1.089506 收敛一致

### 模块 1 `signals-ch1-waveform-transform`（第一章新增，排在卷积之前）
- [x] 数据条目：title「新时刻取到了哪个旧时刻的值」，含预测题、2 组反例、解释题、expected、limitation
- [x] 首版只用梯形 x(τ)=2τ / 2 / 2(3−τ)，特征点 (0,0)(1,2)(2,2)(3,0)；不含指数与门函数预设
- [x] a∈[−3,3] 步长 0.1 默认 1；b∈[−4,4] 步长 0.2 默认 0；**a=0 拒绝并保留上一个有效值**
- [x] 上下轴共用同一横坐标比例与同一 toX；虚线连接四个对应特征点；视野含原/新支撑
- [x] 指标：支撑区间、3/|a|、t₀=−b/a、反折状态；压缩拉伸看 |a|、反折看 a 的符号
- [x] 验收：三组支撑、代回误差 ≤1e-10、a=1,b=0 两波形逐点一致、视野覆盖双支撑
- [x] `tests/practice-waveform.test.mjs`（6 用例）

### 模块 3 `signals-ch1-fourier-synthesis`（第一章新增，排在卷积之后）
- [x] 数据条目：title「谐波逼近和谱线变密是两回事」，含预测题、2 组反例、解释题、expected、limitation
- [x] 单位高度中心对称周期矩形、脉宽 τ=1；T∈[2,10] 步长 0.5 默认 2；N∈[1,101] 步长 1 默认 5
- [x] c_k=(τ/T)sinc(kτ/T)；x_N(t)=c₀+2Σ(k=1…N)c_k cos(2πkt/T)，求和全部整数 k
- [x] 上图：目标、部分和与 1.08949 渐近参考线；不声称有限 N 的峰值不超过该线
- [x] 下图固定 [−4,4] Hz，含全部 |k/T|≤4 的谱点（含零系数）；纵轴 T·c_k、连续包络 τ·sinc(τf)；**保留符号**
- [x] 谱点范围独立于 N，UI 注明「与 N 无关」；MSE 用一个周期 4096 个中点等权
- [x] 验收：谱点数、包络误差、c₂、x_N(0)、过冲区间、偶次系数仅半占空比消失、MSE 随 N 单调下降
- [x] `tests/practice-fourier.test.mjs`（8 用例）

### 批次 C 的守卫与同步
- [x] `tests/notebook-practice-page.test.mjs`：`experiments.length` 8 → **10**
- [x] `tests/practice.behaviour.test.mjs`：短/全标题与 ID 清单改为 10 项；两处下标循环上界改 10
- [x] 保留 `tests/course-content.test.mjs` 的占位符与内容守卫

## 每批验收（文档第五节）
- [x] 数值覆盖默认、两端、临界值与非默认组合
- [x] 390/760/1440px × 浅/深主题逐模块打开：无横向溢出、图轴/图例/数值卡/焦点正常，移动端纵排（本批 12 张，累计 36 张）
- [x] Playwright 操作真实控件验证数据更新；快速切换、播放离开、重进、resize、主题变更、状态保留
- [x] reduced-motion 不自动播放且单步可用；无 console 错误、旧帧覆盖、重复计时器（批次 A 已验；本批两模块无播放循环）
- [x] 报告：文件清单、复算参数、测试数量、截图、性能测量环境、未验证项（`report-batchC.md`）
- [x] **关键数学失败 → 停止下一批**，不放宽容差掩盖（无关键数学失败；5 项变异全部被抓住）

### 批次 C 收尾补充记录
- 变异测试 5 项**全部被抓住**，含文档点名的「删掉傅里叶偶次项」（M3b）与「包络丢符号」（M3c）。`app.js` 还原后 SHA256 一致。
- 性能：模块 1 上限 0.3 ms、长任务 0 条；模块 3 上限中位 5.9 ms，max 27.2 ms（换 N 首帧重算 4096 点 MSE，已按 (T,N) 记忆）；
  1 条 110 ms 长任务**未归因完成**，已列为未验证项。
- 流程修正：批次 B 的「套件与构建并行」瑕疵本批已避免（先构建、后跑套件）。
- 「上下轴相同横坐标比例」仅在代码结构上保证（共用 toX），测试只断言视野覆盖双支撑，未做像素级比例断言（已列入未验证项）。

## 六模块整体状态（批次 C 完成后）
| 模块 | 实验 ID | 状态 | 用例数 |
| --- | --- | --- | ---: |
| 1 波形变换 | `signals-ch1-waveform-transform` | ✅ | 6 |
| 2 动态卷积 | `signals-ch1-convolution` | ✅（批次 A） | 7 |
| 3 傅里叶表示 | `signals-ch1-fourier-synthesis` | ✅ | 8 |
| 4 采样重建 | `signals-ch2-aliasing` | ✅（批次 A） | 6 |
| 5 频谱分析 | `signals-ch2-spectral-leakage` | ✅（批次 B） | 6 |
| 6 循环卷积 | `signals-ch2-circular-convolution` | ✅（批次 B） | 6 |

## 顶部章节导航改版（本轮追加要求）

### 结构
- [x] 一级：章节气泡 `绪论/第1章…第5章`（6 枚），选中胶囊由绝对定位滑块提供
- [x] 二级：一个**大气泡**，只装当前章节的实验芯片
- [x] 同一时间**恰好一枚章节 + 恰好一个实验**处于选中态

### 对齐科目间气泡切换的四处实现（用户明确要求「参考科目间气泡切换」）
- [x] 滑块现场测量，只吃 `transform`/`width`，不依赖动画事件
- [x] 分层与科目一致：按钮填充 z0 < 滑块 z1 < 文字 z2
- [x] 选中态只在一个函数里改，滑块与按钮状态同源
- [x] 内容淡入淡出：沿用 `.notebook-root` 的 `opacity` 140ms 过渡 + 150ms 换内容（与章节正文同一套）；连点清计时器，只有最后一次生效
- [x] `styles.css` 新增 `.practice-chapters` / `.practice-chapter` / `.practice-chapter-thumb` / `.practice-bubble`，含 reduced-motion 规则

### 测试
- [x] 新增 `tests/practice-chapters.test.mjs`（7 用例）：归类顺序、单一选中、滑块落位、淡出淡入可观测、同章节内切换、快速连点只生效一次、reduced-motion
- [x] 同步受影响的助手：6 个模块测试的 `openDemo`、`narrow-practice-chart`/`narrow-practice-layout` 的 `openTab`、`practice.behaviour` 的 `switchTo` 与列表/窄屏用例
- [x] 章节气泡带 `data-experiment-ids`，供按 ID 定位（芯片只渲染当前章节）

### 发现的真实缺陷（测试抓出）
- [x] 章节气泡跨渲染复用 DOM 节点，但点击处理函数闭包捕获了**首次渲染的 chapter**，切换过一次后判断失效 → 改为点击时从 DOM 选中态读当前章节

### 顺带修正的测试缺陷
- [x] `view-switch-slider` 偶发失败（1181 vs 1178）：该断言用固定 500ms 等待，恰落在 `--seg-ease`（cubic-bezier 1.32 过冲）峰值区，实测过冲 3.1px。
      改为等 `getAnimations()` 的 `finished` 再测量，断言意图不变；单跑 5/5 通过。

## 发布边界
- [ ] 迁回 3010 需**用户单独授权**；先查双方未提交改动，按文件暂存、按逻辑变更提交，不用 `git add -A`、不改写历史
- [ ] 推送远端另需授权
