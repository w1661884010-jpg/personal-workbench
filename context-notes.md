# 上下文与实现决策

## 已确认范围

- 课程源目录 `C:\Users\Lenovo\Desktop\learning\课程` 只读，不修改。
- 一级模块固定为信号与系统、数字电子技术、模拟电子技术。
- 信号与系统按本地《信号分析与处理》第3版及配套资料；模电、数电按指定第六版官方章序校准。
- 本地缺少童诗白、阎石第六版完整正文；只使用本地可核对资料，空缺明确显示“资料不足”。
- 不保留今日任务、学习周次、连续学习或任何按日期/时长计算的课程进度；错题页仍按原始需求保留下次复习日期，但不影响课程进度。

## 进度规则

- `ChapterStatus = not_started | in_progress | completed`。
- 课程进度只等于已完成章节数除以计入进度的教材章节数。
- 阅读不会自动完成章节；提交全部章节检验后才解锁完成按钮，不自行增加及格阈值。
- 信号课程绪论和数电绪论不计入分母；模电第0章是正式编号章节，计入分母。
- 旧 v1 数据原样保留，迁移只映射当前章节为学习中，不根据旧掌握度自动完成章节。

## 仿真边界

- 数字工作台使用理想 0/1/X 离散事件模型，不模拟电气时序、亚稳态或器件温度。
- 模拟工作台使用浏览器内受限 MNA 教学模型，支持线性 DC 与 RC 瞬态；二极管、三极管和运放只允许放置、连线与保存，V1 不生成非线性求解结果。
- 不收敛或不支持时必须停止输出并显示可检查诊断，禁止生成猜测波形。
- V1 不做数字/模拟混合信号联仿，不承诺厂商 SPICE 模型、噪声、寄生或热模型。

## 代码边界

- 沿用 React、TypeScript、Vinext/Vite、现有暗色视觉和版本化 localStorage 模式。
- 只替换学习站点的活动数据、视图和进度逻辑；不清理无关的 F28335 历史代码。
- 保留错题与课程连接作为辅助页面，但它们不再参与章节进度。

## 浏览器验收检查点

- 1440px 桌面端：三门课程首页、搜索跳转、章节检验门槛、进度同步、刷新恢复、JSON 导出与导入恢复均通过。
- 数字工作台：空白放置、端口连线、开关 0/1 切换、LED 实时状态、真值表、保存/载入/复制均通过。
- 模拟工作台：5 V 电源与 1 kΩ 负载求得 5 V、5 mA；瞬态示波器显示求解样本；加入二极管后停止求解并清空旧波形。
- 390px 移动端：首页、课程页、横向章节目录、章节长文和工作台均适配；修复后文档无横向溢出。
- 浏览器控制台 error/warn 为 0。

## 发布与迁移检查点

- GitHub 私有仓库 `w1661884010-jpg/personal-workbench` 已建立，主分支保存最终源代码。
- Sites 项目 `appgprj_6a86c36ad44c8191becb7b5965a2c237` 已以 `personal-workbench` 发布，生产页面、课程总览和章节路由验收通过。
- 原项目 `appgprj_6a660bd10df08191932aecfe0b14a0de` 已不在所有者活动站点列表中，按项目 ID 查询返回不存在；当前接口不能进一步区分归档或移除，未用删除替代归档。
- 当前 Vinext 产物是 SSR/RSC 与 Worker，不包含 GitHub Pages 要求的静态 `index.html`；因此未把不可用的 Pages 地址报告为已上线。

## 2026-08-25 画布交互改进决策

- 根因：元件在 `pointerdown` 时直接写入选择状态，没有双击锁定语义；拖动与放置只有画布边界裁剪，没有元件碰撞处理。
- 根因：端口坐标只按输入/输出左右分组，元件模型没有方向字段；连线是位于元件下层的单条贝塞尔曲线，重叠时端点和短连线会被遮挡。
- 根因：SVG `viewBox` 和指针换算固定为 `1200 × 720`，当前没有缩放状态。
- 方向字段采用可选 `rotation` 与 `flipped`，旧存档缺少字段时按 `0°`、未翻转处理，不提升 schema 版本。
- “翻转”实现为水平镜像；“旋转”按 90°步进。标签文字保持正向，元件外形、端口和连线锚点随方向变化。
- 碰撞策略使用带安全间距的网格搜索；放置、拖动和载入旧存档共用同一规则，不自动删除元件或连线。
- 资料不足仍保留状态标签以守住证据边界，但相关正文使用空字符串，界面不渲染空段落；不以新的解释性占位文案替代。
- 首页快捷模块只包含数字电路工作台和模拟电路工作台两个真实入口，不加入新的学习进度或任务体系。

## 2026-08-25 实现检查点

- 新增纯几何模块，覆盖方向变换、端口位置、碰撞分离和带引出段的正交连线路径；旧电路缺少方向字段时仍可载入。
- 工作台已接入双击锁定选择、变换控制、50–200% 缩放、连接端口环和双层连线；拖动不再隐式改写锁定选择。
- 教材名称已去除缺资料括注；`insufficient` 知识讲解正文统一为空且界面不输出空段落。
- 首页两个快捷入口已连接 `sandbox/digital` 与 `sandbox/analog`。
- 几何、存储、课程模型和界面契约共 19 项针对性测试通过，ESLint 通过；完整构建和浏览器验收待执行。

## 2026-08-25 验收检查点

- 完整测试 57 项通过，ESLint、Vinext 生产构建和 `git diff --check` 均通过。
- 桌面浏览器确认：首页快捷入口进入真实工作台；双击锁定选择后可旋转、翻转和缩放；开关连接 LED 后显示 1 条连线、两端连接状态和真实逻辑结果。
- 按用户截图复核直流电源与 BJT：自动放置后保持安全间距，BJT 的 c/b/e 端口分置上、左、下方，未再相互遮挡。
- 390 × 844 移动视口确认：首页课程卡、两个工作台快捷按钮和工作台纵向布局可用；临时视口覆盖已恢复。
- 浏览器控制台 error/warn 为 0；变更将以单一逻辑提交推送并重新发布到既有 Sites 项目。

## 2026-08-25 选中与连线回归诊断

- 复现结论：当前单击只触发了元件的键盘焦点，浅橙色焦点描边被误看作已选中；`selectedComponentId` 仍为空，因此旋转、翻转、删除按钮和 Backspace 都按设计保持禁用。
- 根因：`pointerdown` 把指针捕获设置在外层 SVG，浏览器随后把真实双击重定向给 SVG，元件上的 `onDoubleClick` 没有收到事件。此前合成的 `dblclick` 测试没有覆盖这一浏览器行为。
- 交互约定：单击用于聚焦，拖动用于移动；双击保持选中。键盘 Enter/Space 与双击等价，作为无鼠标操作方式。
- 修复边界：只把指针捕获改由元件自身持有，并移除“新放置即选中”；不改变电路状态、仿真或存储结构。
- 连线样式按用户要求恢复为原来的单条青绿色三次贝塞尔曲线，删除正交路径、深色描边和扩大命中层；已连接端口的环形标记、颜色和文字状态继续保留。
- 成功标准：真实鼠标双击后工具栏立即可用，旋转/翻转可见，按钮和 Backspace 均能删除；单击不锁定；连接仍可删除且节点连接状态清楚可辨。

## 2026-08-25 选中与连线回归验收

- 新增两项界面契约测试先在旧实现上失败，修复后通过；完整自动化测试 58 项、ESLint、Vinext 生产构建和 `git diff --check` 均通过。
- 桌面浏览器以真实鼠标事件确认：单击后 `aria-pressed=false` 且方向/删除按钮禁用；双击后 `aria-pressed=true` 且三个按钮启用。
- 旋转后元件方向为 90°，水平翻转显示“镜像”；Backspace 和“删除所选”按钮都将元件数量减为 0。
- 开关与 LED 连接后只有一个 `.cw-wire` 三次贝塞尔路径，没有 halo/hit 图层；两个端口均带连接环和“已连接”可访问状态。
- 390 × 844 视口下页面无横向溢出，工作台保持单列布局；浏览器控制台 error/warn 为 0。发布与远端同步待完成。
- Sites 版本 5 已成功发布到原有 `personal-workbench` 地址；GitHub 私有仓库 `main` 已更新，远端提交树与本地功能提交树一致。
- 线上地址成功返回站点标题；发布后的第二次深层交互检查遇到浏览器控制超时，因此以已成功的本地真实交互验收和 Sites `succeeded` 状态为发布依据，不将该次超时写成线上交互通过。
- 本次发布归档所在临时目录清理命令被环境策略拦截，未重试更强删除方式；目录仍为 `C:\Users\Lenovo\AppData\Local\Temp\personal-workbench-sites-932c327239774ad0acdd98fd33136d7b`。

## 2026-08-29 数电资料整合决策

- 本轮只修改数字电子技术，信号与系统、模拟电子技术及电路沙盒交互保持现状。
- 内容依据只读目录 `C:\Users\Lenovo\Desktop\learning\课程\模电与数电\02_数字电子技术`；不复制教材 PDF、学习辅导 PDF 或 HDL 工程到站点仓库。
- 教材主线固定为阎石《数字电子技术基础》第六版的 8 章：数制和码制、逻辑代数基础、门电路、组合逻辑电路、半导体存储电路、时序逻辑电路、脉冲波形的产生和整形、数-模和模-数转换。
- “主线必学/选择学习”按后续依赖和基础应用价值划分，不按段落数量凑比例；章节正文采用重新组织的简明表述，不大段抄录教材。
- 本地资料覆盖边界：第 1–6 章有分章笔记或 HDL 实践支撑；第 7 章仅教材主线；第 8 章只有部分 ADC/DAC 笔记。缺少独立实验工程的章节不能标为已有可复现实验。
- 浏览器数字工作台是理想 0/1/X 教学仿真，与本地 ModelSim/SystemVerilog 工程并列说明，不能冒充 HDL 编译、时序签核或器件级仿真。
- 维持现有 v2 学习状态 schema 和全部章节 ID，避免清空用户的章节进度、检验记录与错题引用。
- 现用 GitHub 目标为私有仓库 `w1661884010-jpg/personal-workbench`；现用 Sites 项目为公开的 `personal-workbench`。用户本轮明确选择 Sites 并要求同步到站点，视为对现有公开访问范围内更新的明确授权。

### 成功标准

- 数电总览准确显示 8 章教材顺序、当前章和按已完成章节数计算的进度。
- 每章继续按“学习目标—知识讲解—典型例题—动手实验—章节检验—复习总结”呈现，资料来源状态与实验能力边界准确。
- 教材章、站内章节、浏览器数字工作台和本地 HDL 资料的关联可在页面中读懂并实际跳转。
- 现有 localStorage 记录能继续载入；完整测试无失败或跳过，lint、生产构建与桌面/移动浏览器验收通过。
- 本地提交、GitHub `main` 与 Sites 保存版本来自同一个已验证提交。

### 实现与验证检查点

- 保留 `digital-01` 至 `digital-08` 以及既有章节内 ID；课程进度、章节检验和错题引用无需迁移 schema。
- 第 3–6 章的实验边界分别关联 Appendix_code、ModelSim 参考与 vending_machine 工程；第 7、8 章明确写出 Web 工作台不含 RC/555、真实 ADC/DAC 和 HDL 编译能力。
- 第 7 章已补齐矩形脉冲参数、施密特触发、单稳态、多谐振荡与 555；第 8 章已补齐 D/A 结构与指标、取样保持、并联比较、逐次逼近、双积分和选择学习架构。
- `npm test`：59/59 通过，0 失败、0 跳过；`npm run lint` 与 Vinext/Vite 生产构建通过，`git diff --check` 通过。
- 桌面 1440 × 900：数电 8 章顺序、内容依据、第 7 章公式与工作台跳转可见；页面内容宽度等于视口内容宽度，无横向溢出，控制台 0 error/warn。
- 移动 390 × 844：第 8 章五段知识讲解与实验边界完整，文档内容宽度 375/375，无横向溢出，控制台 0 error/warn。
- PDF 核对产生的 `tmp/pdfs` 为未跟踪临时目录；环境策略拒绝了已验证目标的删除操作，未改用更强删除方式。该目录不会加入提交或远端。

## 2026-08-29 模电资料整合决策

- 本轮只修改模拟电子技术课程内容与对应测试；数字电子技术、信号与系统、学习状态 schema 和电路沙盒实现保持不变。
- 内容依据只读目录 `C:\Users\Lenovo\Desktop\learning\课程\模电与数电\01_模拟电子技术`；不修改或复制教材 PDF、HIT 笔记、USTC LaTeX 源文件及图片到站点仓库。
- 资料索引和教材封面均确认本地完整教材为童诗白、华成英《模拟电子技术基础》第四版，共 651 个 PDF 页面；本地没有第六版正文。本轮选择经过直接核对的第四版主线，不把第四版内容标成第六版，也不推断两个版本的章节差异。
- 第四版目录包含第 0–11 章；现有站点仅有第 0–10 章，因此新增 `analog-11`“模拟电子电路读图”，保留 `analog-00` 至 `analog-10` 及其内部 ID，避免已有进度、检验和错题引用失效。
- 章节知识讲解使用教材规范术语重新组织为简明摘要，不大段照抄；HIT 笔记和 USTC 教程只作为交叉解释与例题补充，并保持来源边界。
- 本地模电资料没有可独立运行的实验工程、网表或 SPICE 项目。每章实验继续链接浏览器模拟工作台，但只验证其线性 R/C、独立源、表计和拓扑记录能力；二极管、BJT、MOSFET、运放、振荡、功放和稳压器均不得描述为真实求解结果。
- “主线必学/选择学习”按后续依赖、基础分析价值和典型应用划分；每章沿用现有 4 项主线、1 项选择的页面契约。
- GitHub 目标仍为私有仓库 `w1661884010-jpg/personal-workbench`；Sites 项目仍为公开的“电路自习室”。用户明确选择该 Sites 项目并要求同步，视为在现有公开访问范围内更新的授权。

### 成功标准

- 模电总览按第四版准确显示第 0–11 章，课程进度按 12 个计入进度的章节计算，已有章节记录继续可用。
- 每章保留学习目标、前置知识、知识讲解、典型例题、动手实验、章节检验、复习总结，以及清晰的 80/20 标记和来源状态。
- 原先资料不足的第 5、8、9、10 章由已核对教材补齐；新增第 11 章读图方法与教材例题范围，不再显示虚假空白或模糊占位说明。
- 模拟工作台跳转可用，实验说明明确没有独立仿真工程和非线性/有源器件求解能力。
- 完整测试无失败或跳过，lint、生产构建与差异检查通过；本地提交、GitHub `main` 与 Sites 保存版本来自同一个已验证提交。

### 实现检查点

- `analog-00` 至 `analog-10` 及其章节内 ID 保持不变；新增 `analog-11`，初始课程进度分母由 11 个教材章更新为 12 个教材章。
- 第 5、8、9、10 章已根据第四版目录与正文范围移除 `insufficient` 空白内容；第 11 章按读图思路、基本方法和四个教材案例组织。
- 第 7 章补入模拟乘法器、有源滤波和信息预处理放大电路；第 8 章补入比较器、非正弦波和运放信号转换，保持教材顺序。
- 全部 12 个实验均关联对应教材章和本地补充资料，并明确本地没有独立实验工程；浏览器工作台不被描述为二极管、晶体管、运放、振荡、功放或稳压器求解器。
- 模电专项课程模型测试 8/8 通过；完整测试、lint、构建和页面验收待执行。

## 2026-08-29 数电与信号内容核验及文案清理决策

- 本轮只修改数字电子技术、信号与系统课程数据及对应测试；模拟电子技术、学习进度 schema 和电路沙盒实现不变。
- 数电依据阎石《数字电子技术基础》第六版目录及 `章节资料映射.md`：现有 8 章章序、章名和主线覆盖正确，无需重建内容体系。
- 信号依据赵光宙《信号分析与处理》第 3 版目录：绪论后为连续信号、离散信号、信号处理基础、滤波器、随机信号分析与处理基础共 5 章。教材明确包含第 5 章，因此“课程范围待确认”和 `insufficient` 状态已经与本地证据冲突，应改为已核对教材内容。
- “清理不必要文本”解释为删除读者无用的实施过程、自我辩解和重复来源前缀，例如“自编例题”“资料关联”“不冒充”“降级实验”“本节只建立地图”；不删除教材名称、工程文件名和会影响实验判断的求解能力限制。
- 例题继续保留为站内重写的最小练习，但标题只保留题目主题；来源边界由课程内容依据和规范化实验限制集中表达。
- 实验说明改用事实句：工作台能验证什么、不能计算什么、可对照哪些 HDL/Notebook 资料。避免“冒充”“降级”等情绪化措辞。
- 保留所有课程、章节、知识段、例题、实验和检验 ID，确保现有 localStorage 进度、检验记录和错题引用继续有效。

### 成功标准

- 两门课程的章序与教材目录一致，信号第 5 章具有已核对正文且不再显示资料不足或范围待确认。
- 页面不再出现上述过程性说明；数电的 HDL/ModelSim 边界和信号 Notebook 的数值近似边界仍可见。
- 课程进度分母和全部 ID 不变；完整测试无失败或跳过，lint、生产构建和差异检查通过。
- 本地提交、GitHub `main` 与 Sites 保存版本来自同一个已验证提交。

### 验收检查点

- `npm test`：60/60 通过，0 失败、0 跳过；`npm run lint` 和 Vinext/Vite 生产构建通过，`git diff --check` 仅待提交前复核。
- 本地预览返回 HTTP 200；默认 1265 × 720 视口显示第四版教材、第 0–11 章、0/12 章节进度和内容依据，控制台 0 error/warn。
- 通过真实章节按钮进入 `#chapter/analog-11`，页面显示 5 段知识讲解、读图例题、实验边界、检验和总结；章节由未学习变为学习中，证明路由与状态处理生效。
- 390 × 844 移动视口的文档宽度为 375/375，无横向溢出；章节目录横向滚动、正文和底部课程导航可见，控制台 0 error/warn；临时视口覆盖已重置。
- 教材 PDF 目录页渲染位于 Windows 临时目录；已验证目标的清理命令被环境策略拒绝，未改用更强删除方式。该目录不在仓库内，也不会进入提交或部署包。

## 2026-08-29 数电与信号内容清理实现检查点

- 数电 8 章、所有 ID 和 80/20 划分保持不变；8 个例题标题已去除“自编”，实验说明改为简洁的能力事实句。
- 数电课程依据保留阎石第六版、分章笔记和 SystemVerilog/ModelSim 工程关系；Appendix_code、vending_machine、HDL 编译、CMOS/TTL 参数、RC/555 与 ADC/DAC 求解边界仍明确可见。
- 信号课程保留绪论和 5 个计入进度的教材章；第 5 章改为 `verified_local`，五段正文分别覆盖随机过程统计描述、功率谱、LTI 系统、最优线性滤波和非平稳分析。
- 全部课程、章节、知识段、实验与检验 ID 未变化，课程进度分母仍为数电 8 章、信号 5 章。
- 新增两项意图测试，专项课程模型测试 10/10 通过，0 失败、0 跳过；完整验证待执行。

### 验收检查点

- `npm test`：62/62 通过，0 失败、0 跳过；信号第 5 章来源、正文主题、进度分母和冗余文案都有可失败的意图断言。
- `npm run lint` 退出码为 0；Vinext/Vite 生产构建完成；`git diff --check` 退出码为 0。
- 本轮只改动两门课程数据、课程模型测试和任务记录；未改动模拟电子技术、状态 schema、页面组件或电路沙盒逻辑。
- 仓库原有未跟踪 `tmp/` 保持原样，不纳入提交或部署。

## 2026-08-30 第六版官方模电教案校准决策

- 正确工作副本为 `C:\Users\Lenovo\Documents\codex_projects\personal-workbench-sites`，其 `.openai/hosting.json` 指向用户指定的“电路自习室”；当前 `站点` 目录属于旧分支，不在本轮修改范围。
- 新增来源只读目录为 `C:\Users\Lenovo\Desktop\learning\课程\模电与数电\01_模拟电子技术\04_官方电子教案`；10 个文件覆盖第 0—9 章，共 508 页，源文件不修改、不复制进站点仓库。
- 官方教案章序为：绪论、常用半导体器件、基本放大电路、集成运算放大电路、放大电路的频率响应、放大电路中的反馈、信号的运算和处理、波形的发生和信号的转换、功率放大电路、直流电源。第 3 章已把多级放大电路列为 3.1，不再作为独立教材章。
- 站点主线改为童诗白、华成英《模拟电子技术基础》第六版；第 0—9 章依据官方电子教案校准。第 10 章“模拟电子电路读图”沿用已核对的本地补充资料，并明确为补充来源，不伪装成已下载的官方教案。
- 保留与内容语义一致的现有章节 ID；删除独立的 `analog-03` 后，将旧记录迁移到合并后的 `analog-04` 并要求重新完成该章检验，防止把只学过半章的记录误标为完成。
- 只修改模电课程数据、必要的学习记录迁移和对应测试；数电、信号、页面结构与电路沙盒不重构。

### 成功标准

- 模电总览按第六版显示第 0—10 章；进度分母为 11 章，第 0—9 章内容与官方教案目录一致。
- 每章继续按学习目标、知识讲解、例题、实验、检验和总结呈现，80/20 标记保持由后续依赖和应用价值决定。
- 模电正文不再出现面向实现过程的“自编例题”“降级实验”“资料关联”或“不冒充”等说明；模拟工作台不支持的器件模型仍以直接能力事实说明。
- 旧 v2 浏览器记录可以迁移，移除的独立多级放大章不会使整份记录失效，也不会被误计为已完成的新第 3 章。
- 自动化测试无失败或跳过，lint、生产构建与差异检查通过；公开 Sites 部署单独等待用户确认。

### 实现检查点

- 模电现按第六版显示第 0—10 章，共 11 个计入进度的章节；原独立“多级放大电路”并入第 3 章“集成运算放大电路”的首段。
- 第 0—9 章为 `verified_local`，第 10 章读图为 `supplemental_local`；第 1 章移除不属于官方教案该章主线的 MOSFET 段，第 2 章继续承担场效应管与派生放大电路。
- 第 6 章信号运算按官方教案收敛到运放运算、模拟乘法器和有源滤波；第 8 章功放按输出功率、导通方式、互补输出级和极限参数组织。
- 学习状态升级为 v3；v2 的 `analog-03` 记录映射到 `analog-04`，合并章固定回到“学习中”并清除旧检验提交，其他章节状态与错题保持。
- 模电课程模型和状态迁移专项测试 15/15 通过，0 失败、0 跳过；完整测试、lint 和生产构建待执行。

### 验证检查点

- `npm test`：63/63 通过，0 失败、0 跳过；包含第六版章序、官方内容主题、来源边界和 v2→v3 迁移意图测试。
- `npm run lint` 退出码为 0；Vinext/Vite 生产构建完成，根页面再次返回 HTTP 200。
- `git diff --check` 退出码为 0；换行提示是仓库既有 Windows 转换策略，不是空白错误。
- 原有未跟踪 `tmp/` 保持不变，不纳入提交或部署包；官方 PPTX 和提取索引均位于仓库外。

## 2026-08-30 GitHub 与 Pages 同步决策

- 用户要求立即同步；范围解释为本地已提交代码、GitHub 私有仓库 `w1661884010-jpg/personal-workbench` 的 `main` 和项目 GitHub Pages。
- 当前本地 `HEAD` 为 `8655838`，比 GitHub `main` 的 `fd08757` 领先 1 个提交；远端尚未配置 Pages，预期网址返回 404。
- 当前 Sites 构建是 Vinext + Cloudflare 服务端产物，不能直接把 `dist/client` 当成完整静态站点发布。GitHub Pages 改用单独的纯 Vite 客户端入口，复用 `LearningWorkbench` 及其直接依赖。
- Pages 配置固定 `base: /personal-workbench/`，避免项目 Pages 子路径下的 JS、CSS、favicon 和社交图资源失效。
- GitHub 仓库必须保持 private；若 Pages API 因套餐限制拒绝私有仓库发布，停止在权限边界处并报告，不将仓库改为 public。
- 原有 `tmp/` 是未跟踪资料处理目录，本次继续排除，不进行顺带清理。

### 成功标准

- 本地与 GitHub `main` 指向同一已验证提交，且提交中不含 `tmp/`。
- Pages 工作流运行测试、lint 和静态构建后成功部署。
- 公开 GitHub Pages 网址返回 HTTP 200，并保留暗色界面、课程路由和 localStorage 功能。

### 本地验证检查点

- Pages 专项契约测试 2/2 通过；完整 `npm test` 为 65/65 通过，0 失败、0 跳过。
- `npm run lint`、`npm run build:pages` 与 `npm run build` 均退出 0。
- 首次使用 `pages/` 作为静态入口时，Vinext 把 `pages/main.tsx` 识别为额外路由 `/main`；已依据完整构建输出改为 `github-pages/`，复测后 Sites 只保留根路由。
- 本地 Pages 预览的首页、JS 和 CSS 均返回 HTTP 200，标题为“电路自习室｜本学期电子类课程个人学习站点”，资源路径均带 `/personal-workbench/` 前缀。

## 2026-08-30 课程完整度与排版统一计划

### Plan

1. 以只读方式核对 `C:\Users\Lenovo\Desktop\learning\课程` 的课程索引、章节映射和模电官方电子教案，不改动源资料。
2. 用代码检查三门课程是否逐章具备目标、前置、讲解、例题、实验、检验和总结，并定位知识讲解过短或缺少必要公式的核心段。
3. 删除读者不需要的“资料已核对、补充资料、内容依据、资料状态”等整理过程说明；保留会影响实验判断的真实仿真能力边界。
4. 只调整课程总览和章节学习页所需组件、样式、课程数据与测试；不改学习进度、localStorage schema 或电路沙盒逻辑。
5. 在完整自动化检查和桌面/移动浏览器验收通过后，提交同一源代码状态并同步 GitHub、GitHub Pages 和现有 Sites 项目。

### 假设与成功标准

- “去除所有临时文本说明”解释为移除资料整理和实现过程元数据，不删除教材名称、主线/选择标记、学习规则和仿真能力限制。
- “完善知识内容”以现有教材、教案、分章笔记和配套资料为边界，优先补足关键条件、分析步骤与公式含义，不新增课程外体系，也不大段转录源资料。
- 截图问题的直接原因是章节侧栏编号列仅 `33px` 且允许中文换行；修复后“第1章”等编号必须保持单行，标题列仍可正常换行。
- 成功标准：三门课程结构完整；页面不再显示来源状态类临时说明；章节侧栏在常见桌面宽度不折行；桌面与移动端无横向溢出；测试、lint、两套构建均无失败或跳过；生产站点发布成功。

### 实现检查点

- 结构审计确认信号课程 6 个学习单元、数电 8 章、模电 11 章均具备七段式内容；每章保持 4 项主线和 1 项选择学习。
- 课程总览右栏改为“当前章重点”和“内容结构”；章节页移除来源状态徽标，Notebook 实验改用直接的使用说明。
- 章节总览与章节学习侧栏的编号列统一为 `44px` 并禁止换行，标题列使用 `minmax(0, 1fr)` 保持可收缩。
- 数电补入德摩根、全加器、触发器特性方程、状态方程和采样/量化关系；模电补入二极管方程、小信号增益、反馈、运放、振荡、功放和桥式整流关系。
- 新增意图测试覆盖临时文案清理、章节编号单行排版和十二个关键公式；数电、模电、信号与界面专项测试 21/21 通过。

### 验证检查点

- `npm test`：67/67 通过，0 失败、0 跳过；`npm run lint`、Vinext Sites 构建、Vite Pages 构建和 `git diff --check` 均退出 0。
- 1440 × 900 桌面实测中，课程侧栏和学习路线的“第1章”等编号宽度均为 44px、保持单行，页面横向溢出为 0；浏览器正文未检出资料整理类临时文案。
- 通过真实按钮进入信号第 1 章和模电第 1 章；七段式学习顺序完整，卷积关系与二极管方程可在章节页面读取。
- 发现章节顶部状态徽标因 flex 默认拉伸而过高；已增加 `align-self: flex-start`、禁止收缩和禁止换行，复验高度由 94px 恢复为 25px。
- 390 × 844 移动实测中，顶部搜索、横向章节目录、正文卡片和底部课程导航均可用；页面横向溢出为 0，状态徽标保持 50 × 25px。

## 2026-08-31 审计报告整改计划

### Plan

1. 以修订定稿的章节完整度审计为问题清单，先补三门课程中有明确资料依据的高优先级知识缺口。
2. 统一教材映射、公式展示和搜索入口，不改变三门课程章序或现有暗色视觉结构。
3. 将章节检验改为明确及格门槛，答错题自动进入错题复盘，同时迁移已有示例记录。
4. 让已有实验预设真正载入画布，并在实验卡和工作台明确“可求解”或“仅拓扑/手算”。
5. 运行完整测试、lint、Sites/Pages 构建和差异检查；公开发布前取得用户确认。

### 决策与边界

- 课程源资料继续只读；不建立第二套内容体系，不按篇幅机械扩写。
- 章节完成门槛改为检验得分至少 60%；旧版已完成章节不倒退，旧检验记录保留并允许按新题目重新提交。
- 错题记录增加来源类型；初始记录明确标注为示例，数电示例归到逻辑代数章，模电示例归到基本放大电路章；检验错题使用稳定题目 ID 自动去重。
- 公式采用 KaTeX 数学排版；解析失败时回退显示原始文本，搜索同时覆盖公式和变量解释。
- P2 采用可验证降级：数字实验和线性模拟实验可载入真实画布预设；含二极管、BJT、运放的实验明确显示“仅拓扑/手算”，不扩张为未经验证的非线性求解器。
- 信号实验仍以课程数值步骤为依据；本轮不内嵌 Python/WASM Notebook，也不把提示信息或动画称为可运行 Notebook。
- Sites 当前为既有公开站点；完成本地验证后，按 Sites 托管规范在公开部署前取得用户确认。

### 成功标准

- 审计列出的核心内容缺口有对应正文、公式或例题补强，且教材术语与当前资料一致。
- 公式可读、可搜索；例题答案默认隐藏；章节未达到 60% 时不能完成。
- 错误检验题自动形成可编辑错题，示例错题有清晰标识且章节正确。
- 现有 `presetId` 不再是死引用；能够求解的实验有真实预设，不支持的实验不生成猜测结果。
- 自动化测试无失败或跳过，lint、Sites 构建、Pages 构建与差异检查全部通过。

### 实现检查点

- 信号课程补齐连续傅里叶变换常用性质、相关定理、DFT/FFT 与频谱泄漏、滤波器转换及随机过程辨析；数电补齐补码运算、多输出化简、PLD/HDL、555 和转换器指标；模电补齐器件、偏置、差分、频响、反馈、运算、振荡、功放和电源主线。
- 公式改用 KaTeX 排版并纳入全局搜索；典型例题答案默认折叠。
- 学习记录升级为 v4：章节检验 60% 及格，错误答案自动生成稳定 ID 的错题，重新答对后标记已复盘；v3/v2/v1 记录均保留迁移入口。
- 新增 3 个数字电路和 5 个线性模拟电路真实预设；其余模拟实验明确只做拓扑和手算，信号 Notebook 不再保留不可执行的预设引用。

### 验证检查点

- `npm test`：71/71 通过，0 失败、0 跳过；包括全部公式 KaTeX 解析、60% 完成门槛、自动错题、v4 迁移和 8 个预设真实求值。
- `npm run lint`、Vinext Sites 构建、Vite GitHub Pages 构建、`git diff --check` 均退出 0。
- 源码临时文本扫描未发现“教材正文未找到”“待并入”“内容待补”“TODO/FIXME”等读者可见占位内容；仅测试中保留相应禁止出现的断言。
- Pages 构建提示主入口约 658 kB，属于非阻断体积警告；本次未为该警告扩张到代码拆分重构。

## 2026-09-01 课程总览单章导读计划

### Plan

1. 先以独立提交调整首页区块顺序，不改变快捷入口文案、跳转或课程卡片结构。
2. 为课程总览增加仅存在于界面层的选章状态；目录点击只预览，唯一主按钮才沿用 `openChapter` 写入学习状态并跳转。
3. 将课程总览中央区域收敛为单章导读，复用现有章节正文、公式、例题、实验、检验和提交记录；右栏只保留课程级说明。
4. 提取共享 KaTeX 组件，按既有实验预设规则展示能力，不解析 `limitation` 文案。
5. 更新响应式样式和意图测试，完成自动化、桌面/移动浏览器、GitHub Pages 与 Sites 发布验证。

### 决策与边界

- 本轮只实施最终提示词中的 P0；不新增教材节号映射，不改课程数据、状态 schema 或仿真器。
- `selectedChapterId` 是界面预览状态，不进入 localStorage；课程切换时清空，中央回落到该课程真实当前章。
- 左栏选中项与真实当前学习章是两个概念：选中项控制中央导读，真实当前章才显示“当前学习”。
- 实验能力使用 `workbench`、`presetId` 与 `getCircuitPreset` 判定；Notebook 显示步骤验证，有真实预设才显示可载入运行。
- 核心节正文完整展示，选择学习节使用原生 `details` 折叠；不添加装饰性进度条或额外行动按钮。
- 公式渲染从章节页私有实现提取为共享组件，保持 KaTeX 参数与无障碍标签不变。
- 提交边界固定为两个：首页顺序；课程总览交互、公式复用、样式和测试。

### 成功标准

- 首页工作台快捷入口位于课程标题与课程卡片之前，桌面和移动顺序一致。
- 课程总览只保留一份章节目录，中央一次只显示一个章节，右栏不重复章节内容。
- 目录预览不调用 `startChapter`、不改变路由或学习记录；主按钮按当前/非当前显示继续或开始，并进入章节页。
- 三态实验能力、检验得分、绪论不计进度、公式显示和课程切换重置均与现有数据一致。
- 完整测试无失败或跳过；lint、Sites/Pages 构建、桌面与移动浏览器验收、GitHub Pages 和 Sites 部署全部通过。

## 2026-09-01 中性灰主题替换计划

### Plan

1. 以参考图的深灰工作区为视觉基准，只替换共享主题、课程强调色、Canvas 图示和工作台硬编码颜色。
2. 保留现有信息层级、圆角、排版、交互与课程内容；不借配色调整进行组件重构。
3. 用自动化测试锁定新主题色，再检查首页、课程总览和电路工作台的桌面/移动端可读性与状态反馈。
4. 完成单一语义提交并同步 GitHub Pages 与既有 Sites 站点。

### 决策与边界

- 主色采用炭灰背景 `#181a1e`、灰黑面板 `#22252a`、次级表面 `#2a2d33`、冷灰边线 `#373b42`；正文保持接近白色，辅助文字使用中灰。
- 为避免无关重构，继续复用现有 `--teal*` 变量名，但其值改为钢灰体系；所有用户可见青绿硬编码同步替换，不再显示青色主强调。
- 橙色继续只表示待复习、警告和错误；示波器多轨迹与课程连接中的少量蓝/橙用于辨识数据，不作为页面主色。
- 课程入口改用三个明度不同的灰色，保留课程辨识但不恢复彩色主题。
- 成功标准：首屏明显呈中性灰而非蓝绿色；文本、按钮、焦点、选中、连线和端口状态仍清晰；布局和功能无回归；测试与两套构建全部通过。

### 实现检查点

- 首页快捷入口已独立提交；课程总览仅保留左栏一份章节目录，中央展示单个章节的重点、知识链、完整核心正文、实际公式、折叠选修内容与资源状态，右栏只保留课程级信息。
- 章节预览使用 `selectedChapterId` 本地状态；导航、初始化和浏览器历史切换都会清空预览，只有中央主按钮沿用 `openChapter` 启动章节并跳转。
- KaTeX 渲染提取为 `MathFormula`，章节学习页与课程总览共用同一组选项和无障碍公式标签。
- 实验能力只按 `workbench`、`presetId` 和真实预设判定，未解析 `limitation` 文案，也未增加实验跳转。

### 验证检查点

- `npm test`：72/72 通过，0 失败、0 跳过；`npm run lint`、Vinext Sites 构建、Vite Pages 构建与 `git diff --check` 均退出 0。Pages 构建仅保留既有的大包体积非阻断警告。
- 1440 × 900 桌面实测为 220 / 615 / 260 px 三栏，全页只有 1 份章节目录、0 份旧路线列表；共享公式以 KaTeX 正常渲染。
- 点击信号第 1 章预览后 URL 仍为 `#course/signals`、真实当前章仍为绪论且保存状态不变；点击唯一“开始学习本章”后进入 `#chapter/signals-ch1` 并显示学习中。
- 先预览信号第 2 章、切换数电再回到信号后，中央恢复真实当前的信号第 1 章；三类实验标签分别验证为步骤验证、自由搭建核对和预设可载入运行。
- 390 × 844 移动实测中章节目录为横向可滚动选择器，中央与右栏纵向排列，页面无横向溢出、中央只有 1 个主按钮，控制台无错误。

### 中性灰主题验证检查点

- `npm test`：72/72 通过，0 失败、0 跳过；`npm run lint`、Vinext Sites 构建、Vite Pages 构建与 `git diff --check` 均退出 0。Pages 构建仅有既存的单包体积非阻断警告。
- 1440 × 900 桌面实测中，页面背景为 `rgb(24, 26, 30)`，面板为 `#22252a`，未发现旧青色渲染节点或横向溢出；首页、信号课程总览和数字工作台均保持清晰层级。
- 数字工作台双击元件后，旋转、水平翻转和删除按钮均恢复可用；选中元件、端口和连线采用灰白反馈，控制台无警告或错误。
- 390 × 844 移动实测显示底部课程导航，页面无横向溢出，灰色主题与桌面一致；临时视口已在检查后重置。

## 2026-09-01 跟随系统的明暗主题计划

### Plan

1. 保留现有石墨灰深色主题，新增同一灰阶语言的浅色变量，不改排版、课程内容或导航结构。
2. 顶栏增加一个紧凑外观按钮，按“跟随系统 → 深色 → 浅色”循环；按钮文字、图标与无障碍名称同步显示当前选择。
3. 用户选择写入独立的 localStorage 键；初始无记录时跟随系统，系统模式下监听 `prefers-color-scheme` 变化。
4. 在页面脚本执行前设置解析后的主题，避免首屏先暗后亮；验证持久化、系统响应、移动端空间与现有功能不回归。

### 决策与边界

- 主题偏好只属于设备本地界面设置，不并入课程 JSON 备份，也不改变现有学习记录 schema。
- 共享页面、侧栏、顶栏、卡片、表单与状态色都响应明暗模式；课程示意图和电路画布作为“仪器屏”继续使用深色底板，保证波形、端口和连线对比稳定。
- 不增加设置页、弹窗或新依赖；单个按钮循环三态是当前顶栏和移动端空间内最小且可发现的实现。
- 成功标准：无已保存偏好时按系统显示；三态均可切换并刷新保留；系统模式随系统变化；浅色文字、边界和警告可读；全部测试与两套构建通过。

### 实现检查点

- `app/lib/theme.ts` 统一三态顺序、标签、校验与解析；界面偏好使用独立键 `personal-workbench-theme`，不进入课程备份。
- 根布局在首屏渲染前读取偏好并设置 `data-theme`；客户端复用同一解析规则，并仅在“跟随系统”时响应系统配色变化。
- 顶栏外观按钮显示当前模式图标与文字，按“跟随系统 → 深色 → 浅色”循环；移动端沿用既有紧凑图标规则。
- 全局共享变量使用浅/深双值；电路画布与课程示意图继续使用固定深色仪器底板。针对主题的 17 项 UI 意图测试已通过。
- GitHub Pages 的静态 HTML 同样在应用脚本前解析偏好，避免只有 Sites 版本具备无闪烁初始化。

### 验证检查点

- `npm test`：76/76 通过，0 失败、0 跳过；包含三态循环、系统解析、存储值校验和两种发布入口的主题初始化契约。
- `npm run lint`、Vinext Sites 构建、Vite Pages 构建与 `git diff --check` 均退出 0；Pages 构建仅保留既有的大包体积非阻断警告。
- 本地开发入口 `http://localhost:3000/` 返回 HTTP 200。依据 Sites 项目的专用预览规则，本轮未在用户未明确要求浏览器测试时执行 DOM、截图或点击式浏览器 QA；外观按钮行为由意图测试与纯函数测试覆盖。
